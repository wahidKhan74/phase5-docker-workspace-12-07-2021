step 1: create a ci server (jenkins)
       > create ec2 instance
       > Connect to ec2 instance
       > install jdk & maven
       > install jenkins server
       > configure jenkins for admin user.
       > install all suggessted plugins.
       > "http://52.90.194.191:8080/"

step 2: Create a docker server

step 3: Configure a CI Pipeline
        > Create with  manual config   > create a jenkins job
        > Create a declerative Pipeline  -> Jenkins file.